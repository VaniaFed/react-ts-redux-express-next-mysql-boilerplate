import {
    fetchUserRequest,
    fetchUserSuccess,
    fetchUserError
} from 'actions/fetch-user';
import { setUsername } from 'actions/set-username'

export const actions = {
    fetchUserRequest,
    fetchUserSuccess,
    fetchUserError,
    setUsername
};
